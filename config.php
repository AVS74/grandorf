<?php
/**
 * config.php — настройки уведомлений о заказах.
 *
 * ВАЖНО:
 *  — этот файл содержит пароль от почты и токен Telegram-бота,
 *    поэтому НЕ публикуйте его и не делитесь им;
 *  — вставьте свои значения вместо ВСТАВЬТЕ_... и загрузите файл
 *    в ту же папку, что и order.php (обычно рядом с index.html);
 *  — файл специально называется config.php (а не .json/.txt), чтобы
 *    веб-сервер исполнял его как PHP и не отдавал содержимое напрямую
 *    по ссылке — но лучше дополнительно закрыть доступ через .htaccess
 *    (см. пример deny.htaccess рядом).
 */

return [

    // --- Почта (SMTP) для отправки уведомлений о заказах ---
    'smtp' => [
        'host'       => 'mail.hosting.reg.ru',
        'port'       => 465,
        'secure'     => 'ssl',                 // 'ssl' для порта 465, 'tls' для 587
        'username'   => 'orders@grandorf74.ru',
        'password'   => 'ВСТАВЬТЕ_ПАРОЛЬ_ОТ_ПОЧТЫ',
        'from_email' => 'orders@grandorf74.ru',
        'from_name'  => 'Grandorf74 — заказы',
    ],

    // Куда слать уведомления о новых заказах. Можно указать несколько адресов.
    'notify_emails' => [
        '323300@gmail.com',
    ],

    // --- Telegram ---
    'telegram' => [
        'enabled' => true,                     // false — временно отключить уведомления в Telegram
        'token'   => 'ВСТАВЬТЕ_ТОКЕН_БОТА_ЗДЕСЬ', // получен через @BotFather
        'chat_id' => '1878725082',
    ],

    // Контактный телефон магазина — используется в текстах ошибок,
    // если заказ не удалось отправить автоматически.
    'shop_phone' => '+7 (962) 485-46-25',
];
